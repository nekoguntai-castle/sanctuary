/**
 * Stored classifications below this version are repair candidates.
 * Increment only when persisted ownership/type/amount semantics change.
 */
export const CURRENT_TRANSACTION_CLASSIFICATION_VERSION = 2;
