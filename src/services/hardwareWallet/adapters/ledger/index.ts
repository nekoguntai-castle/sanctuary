/**
 * Ledger Adapter - Barrel Export
 *
 * Re-exports the public API so that existing imports
 * (e.g., `from './ledger'`) continue to work unchanged.
 */

export { LedgerAdapter } from './ledgerAdapter';
