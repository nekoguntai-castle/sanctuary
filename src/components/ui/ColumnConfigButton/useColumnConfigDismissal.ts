// Backward-compatible alias. The implementation now lives in the shared
// hooks/useDismissable.ts so multiple overlays reuse one tested copy.
export { useDismissable as useColumnConfigDismissal } from '../../../hooks/useDismissable';
