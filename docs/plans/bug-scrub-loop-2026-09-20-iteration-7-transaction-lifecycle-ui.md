# Bug Scrub Loop Iteration 7 — Transaction, Lifecycle, and Transfer Search

Source target: `69d7e0fa67b2d6727d55463ea1c32b412f70f4d3` (`origin/main`). Scope: resolve every P0–P2 finding accepted by the complete iteration 7 whole-repository scrub. Deployment remains deferred until the loop's final zero-finding pass.

## Accepted findings

- **P2 — `rbf-replacements--double-count-ledger-stats-export`:** an RBF original remains stored after replacement and is included in running balances, counts, amount/fee aggregates, last-transaction balance selection, and CSV/JSON export. The visible transaction list already treats `rbfStatus = replaced` as non-live.
- **P2 — `transaction-export--effective-date-range`:** export serializes `blockTime ?? createdAt` but filters only `blockTime`, which excludes pending transactions whenever a date range is selected. The end-date parser also combines UTC date parsing with local `setHours`, truncating the selected UTC date under non-UTC server time zones.
- **P2 — `transfer-ownership-modal--stale-user-search`:** ownership-recipient searches have no request generation. Late results and `finally` blocks can replace the current query's results/loading state, reappear after the query is shortened or selection is cleared, and supply the `toUserId` used to initiate a transfer.

## Phase 1 — Enforce one live-transaction contract

- [x] Add red repository/API regressions with a received row, a persisted replaced RBF debit, and its active replacement. After `recalculateBalancesAtomically`, the live replacement/final balance must exclude the original; transaction stats, wallet stats, assistant/LLM counts and type aggregates must count only live logical transactions; fee totals must exclude the original. Assert both the internal AI spending-velocity endpoint and the assistant/intelligence velocity path exclude the original from count and amount. Confirm the replaced row retains audit linkage and receives no live `balanceAfter` value. For the agent dashboard, make a newer replaced spend rank ahead of an older active spend and prove the active spend remains visible. For confirmation refresh, prove a replaced-only row neither admits its wallet nor returns from any of the four wallet/row selectors, while an active row still satisfies both shallow-confirmation and lower-tip branches. Cover core repositories, agent dashboard and confirmation updater suites, and the separate `assistantRead/transactionReads.ts` and `assistantRead/walletReads.ts` implementations used by transaction stats, wallet detail/search, dashboard counts, and fee analytics.
- [x] Define a reusable Prisma live-transaction predicate for `rbfStatus != replaced` and apply it to the generic wallet count/type/fee/last-row queries used by transaction stats, wallet stats, LLM endpoints, and recalculation endpoints, including `transactions/core.ts::aggregateSpending` and `intelligenceRepository.getTransactionVelocity`. Apply the same invariant to the duplicated assistant list/search/stats/count/fee queries, ANDing it with caller-supplied filters so a caller cannot override live status; filter the agent dashboard's recent spends and the confirmation-refresh candidate/row selectors; retain explicit txid detail/audit lookup, label backup, physical support counts, confirmed-only history, and historical RBF linking methods. In the SQL window recalculation, make replaced rows contribute zero and assign them `balanceAfter = NULL`, while live rows retain chronological running balances.
- [x] Extend the existing RBF repository mutations to queue durable balance repair whenever live membership changes, and make unchanged sync detect legacy replaced rows whose stored balance predates replacement-aware accounting. Add real PostgreSQL tests proving an `rbfCleanupPhase` status-only transition queues repair, unchanged sync discovers legacy rows without a marker, and recalculation clears/nulls the polluted derived balances without deleting RBF audit linkage. Keep the migration tree unchanged because the sealed wallet-sync replay gate binds the immutable release-candidate image to its current migration digest.
- [x] Re-read every changed repository caller and the confirmed-only dashboard history/activity queries. Keep confirmed bucket history semantics unchanged. Run focused repository, wallet/query, transaction stats, RBF broadcast, and real PostgreSQL integration tests with signed cleanup.

Acceptance: one logical RBF spend contributes once across balances, stats, and assistant/LLM surfaces; the replacement's running balance agrees with UTXO balance in the regression; future repository-owned status-only RBF transitions durably admit balance repair and legacy rows self-admit on unchanged sync; audit linkage remains queryable; confirmed-only analytics remain unchanged.

Backout: before deployment, revert the phase PR. After replacement-aware accounting has run, do not restore code that double-counts replaced rows; fix forward. A forward backout may disable new repair admission only after legacy replaced balances have been consumed. No transaction or RBF audit row is deleted.

Phase 1 implementation verification: the final adversarial review and post-redesign delta reviews were clean. The final full server and coverage run passed 716 files and 16,415 tests with 100% statements, branches, functions, and lines. The PostgreSQL reconciliation suite passed 37 tests with signed cleanup `cleaned`, including production `rbfCleanupPhase` admission, clientless replacement-link repair admission, unchanged-sync repair of a legacy unmarked row, exact repair admission for authoritative or raw batch membership changes, confirmed-original demotion of its losing replacement even when that loser carries stale confirmed state, wallet-lock serialization for caller-owned and per-chunk clientless bulk RBF reconciliation plus membership and multirow patches, caller lock-timeout restoration, duplicate patch last-write behavior, atomic rollback when a requested patch row is missing, and cross-wallet linkage rejection. A separate PostgreSQL balance-bucket test proved replaced rows are excluded and also completed with signed cleanup `cleaned`. Root app/test/all typechecks, the 652-file frontend suite with 8,751 tests, 100% frontend coverage, production build, and lint all passed.

## Phase 2 — Use effective transaction dates in export filters

- [x] Add red export regressions for a persisted replaced RBF debit plus its active replacement, an unconfirmed row whose `createdAt` is inside the requested range, a confirmed row at the last millisecond of the end date, an outside row, start-only/end-only ranges, malformed/non-calendar dates, and reversed bounds. Assert only the live replacement is exported and assert exact `00:00:00.000Z` through `23:59:59.999Z` bounds under UTC and a non-UTC `TZ`.
- [x] Add an export query schema through `validate({ query: ... })` or the repository's equivalent. Preserve current format/default behavior, reject malformed/non-calendar/reversed dates with the standard HTTP 400 invalid-input envelope, and document that response in OpenAPI. Construct inclusive UTC boundaries without local-time mutation.
- [x] Reuse phase 1's live-transaction contract in `findExportRowPage`. Implement a parameterized SQL page query, or a proven equivalent, on the existing repeatable-read transaction client. Use the exact effective-date predicate and total order `COALESCE("blockTime", "createdAt") ASC, "id" ASC`, bind wallet/range/limit/offset values, and exclude `rbfStatus = replaced` without changing output fields, page size, backpressure, permit, or cleanup behavior. Add real PostgreSQL cases for equal effective timestamps across confirmed/pending rows and a mixed set larger than one page to prove no skip or duplicate. Measure the representative query under the 30-second capture deadline and add an expression index only if evidence requires it.
- [x] Re-read every changed export caller and serializer. Run focused export serialization/snapshot/stream, HTTP/OpenAPI, and real PostgreSQL integration tests with signed cleanup.

Acceptance: CSV/JSON export contains only the live replacement; inclusion, total ordering, pagination, and UTC date boundaries use the displayed effective date; invalid ranges return the documented 400 envelope; output and streaming behavior remain compatible.

Backout: revert this phase PR while retaining phase 1's replacement-aware accounting and durable repair admission. No data migration belongs to this phase.

Phase 2 implementation verification: the full server coverage run passed 716 files and 16,420 tests with 100% statements, branches, functions, and lines. The focused transaction HTTP suite passed 109 tests, including equal-day and legacy format-fallback regressions. The PostgreSQL effective-date paging regression passed with 503 live rows over two pages in under the 30-second capture deadline and signed cleanup `cleaned`; that representative query did not require an expression index. A proposed index was rejected because this repository's release proof seals the migration tree, so no migration is included. Root `typecheck:all`, server lint, and the foreground pre-commit gate passed. An independent adversarial review found no P0-P2 correctness issues.

## Phase 3 — Fence ownership-recipient searches

- [x] Add deferred-promise component regressions for `al` followed by `alice` resolving in reverse order; a request resolving after the query shrinks below two characters; and a request resolving after select then clear. Add a completed query A followed by pending query B and prove query A's rows disappear immediately and cannot be selected or submitted. Assert only the current generation may write results or loading state.
- [x] Add a monotonically increasing request generation to `useTransferOwnershipModal`. Increment it for every input change and selection/reset action that invalidates results. Clear `searchResults` synchronously on every query change before starting a valid search, as well as for short queries. Gate success, failure logging/state, `finally`, and selection writes to the current generation, following the existing device-sharing search ownership pattern. Clear searching immediately for short queries and selection/reset transitions.
- [x] Preserve wallet/device modal mount behavior, recipient selection, reversible three-step transfer semantics, API shape, and submit busy state. Run the transfer modal tests plus wallet/device modal and route-ownership regressions.

Acceptance: displayed recipients always belong to the current query generation; once the input changes, no row from the prior query remains selectable while the current request is pending; stale completions cannot clear a newer spinner or reappear after selection/reset; the selected `toUserId` must have come from current visible results.

Backout: revert the phase PR. No server or data change is involved.

Phase 3 implementation verification before delivery: the transfer modal suite passed 38 tests, including reverse-order success, stale rejection, short-query invalidation, stale spinner ownership, selection/reset invalidation, immediate prior-result removal, and rejection of a recipient ID outside the current visible result set. The transfer modal plus wallet/device route-ownership suites passed 64 tests. Root app, test, and aggregate typechecks, app lint, architecture boundaries, and the large-file classifier passed. The full 652-file frontend suite passed 8,758 tests with 100% statements, branches, functions, and lines. An independent adversarial review found no P0-P2 correctness issue. Its selection-gating recommendation was implemented as current-visible ID membership with the canonical current result object, avoiding reliance on render object identity.

## Cross-phase delivery and verification

- [ ] Before each phase, fetch `origin/main`, recheck open PRs and exact source paths, and repeat plan review if the source contract changed materially. Deliver phases serially through protected PRs; verify exact head CI, merge ancestry/tree, and exact merge-SHA target CI before starting the next phase.
- [ ] Run server typechecks and the full server suite; server unit coverage at 100%; root app/test/all typechecks, build, full tests and 100% coverage; lint, architecture boundaries, integration manifest check, large-file and complexity gates. Run every database-backed regression through `scripts/run-integration-tests.sh` and require signed cleanup `cleaned`.
- [ ] Run an independent adversarial implementation review for each code phase and the foreground pre-commit hook under Node 24.21.0/npm 12.0.2. Record exact commands, counts, PRs, merge SHAs, target workflows, cleanup receipts, and any rejected review comments in this plan.
- [ ] After all implementation phases land, mark all three findings resolved only with verified merge and target-CI evidence. Commit the completed delivery record through its own verified PR if it changed after the final code merge. Keep the running stack unchanged until a later full scrub produces the loop's zero-P0–P2 termination pass.

Completion requires all three findings resolved at `origin/main`, no unreviewed behavior expansion, clean resource receipts, and verified target CI for every phase. A fresh whole-repository rescrub remains mandatory; this plan cannot itself close the outer loop.
